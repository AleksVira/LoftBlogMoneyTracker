<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Container">
        <item name = "android:layout_width">match_parent</item>
        <item name = "android:layout_height">match_parent</item>
        <item name = "android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name = "android:paddingRight">@dimen/activity_horizontal_margin</item>
        <item name = "android:paddingTop">@dimen/activity_vertical_margin</item>
        <item name = "android:paddingBottom">@dimen/activity_vertical_margin</item>
        <item name="android:textSize">@dimen/medium_main_text</item>
     </style>

    <style name="Container.Background">
        <item name = "android:background">@drawable/green_macro_leaf</item>
    </style>

    <style name="Container.YellowBackground">
        <item name = "android:background">@color/yellow</item>
        <item name="android:textSize">@dimen/medium_small_text</item>
    </style>


</resources>
